export default (req, res) => {
  if (req.method === "POST") {
    //TODO: add category handling
  }
  if (req.method === "PUT") {
    //TODO: edit category handling
  }
  if (req.method === "DELETE") {
    //TODO: delete category handling
  }
};
