export default (req, res) => {
  if (req.method === "POST") {
    //TODO: add user handling
  }
  if (req.method === "PUT") {
    //TODO: edit user handling
  }
  if (req.method === "DELETE") {
    //TODO: delete user handling
  }
};
