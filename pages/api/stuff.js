export default (req, res) => {
  if (req.method === "POST") {
    //TODO: add stuff handling
  }
  if (req.method === "PUT") {
    //TODO: edit stuff handling
  }
  if (req.method === "DELETE") {
    //TODO: delete stuff handling
  }
};
